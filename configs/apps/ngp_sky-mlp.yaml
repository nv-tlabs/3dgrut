# @package _global_

# order in which configs override each other (/* - denotes a relative search path)
defaults:
  - /apps/ngp
  - /background@_global_: sky_mlp
  - _self_

dataset:
  use_aux_data: true
