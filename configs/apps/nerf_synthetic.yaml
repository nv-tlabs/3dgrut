# @package _global_

# order in which configs override each other (/* - denotes a relative search path)
defaults:
  - /base.yaml
  - /dataset: nerf
  - /initialization: random
  - _self_

# overwrite of default parameters

val_frequency: 1000

dataset:
  train:
    sample_full_image: True

    ray_jittering:
      type: stratified             # Choose from: 'none', 'stratified', 'random'
      start_iteration: 500         # Start jittering from iteration N
      num_samples: 16              # 'stratified' jittering only -- number of subsamples to use in pattern
      apply_every_n_iterations: 1  # 1 for applying every iteration, 2 for applying every other iteration

model:
  background:
    name: background-color
    color: black
